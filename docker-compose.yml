services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: geospatial-quality-backend
    ports:
      - "8000:8000"
    volumes:
      - backend_uploads:/app/uploads
      - backend_outputs:/app/outputs
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - DEBUG=True
      - ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://frontend:80
      - MAX_UPLOAD_SIZE_MB=50
      - UPLOAD_DIR=./uploads
      - OUTPUT_DIR=./outputs
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: geospatial-quality-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    env_file:
      - ./frontend/.env
    restart: unless-stopped

volumes:
  backend_uploads:
  backend_outputs:
